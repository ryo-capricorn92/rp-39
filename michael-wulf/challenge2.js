function solarSystem(age,distanceFromCenter, knownComets, planets, size, distanceFromSun, satellites) {
    return {
        age: "4,600,000,000",
        distanceFromCenter: 27000*Math.pow(10, 1000),
        knownComets: 30406,
                    }

function planets (name, distanceFromSun, knownComets,) {
    name: name,
    distanceFromSun: distanceFromSun,
    knownComets: knownComets
}

        };

var Mercury = planets ("Mecury", 36000000, 470);

//number 2

function range (start, end) {
        display=0
        for (var i = 0; start < end.length; i++);
        return display+i+range(start[i]);
}

range (0,4);     

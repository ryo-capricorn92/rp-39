//exercise 1
function billTotal(subtotal) {
    return subtotal + (subtotal * .15) + (subtotal * .095);
}
billTotal(10); // returns 12.45
//exercise 2
function animalNoise (animal, emotion) {
    if (animal === cat) && (emotion === happy); {
        return "Prrrrr";
        } else if (animal === cat) && (emotion === hungry); {
        return "Meow";
        } else if (animal === dog) && (emotion === happy); {
        return "Ruff";
        } else (animal === dog) && (emotion === hungry); {
        return "Grrrrr";
    }
}
animalNoise(cat, hungry); // not working
//exercise 3
function digitalSum(n) {
    if n >= 0; {
        return n
        // n[1];
        // n[2];
}
digitalSum(25); // not finished and not sure how to approach the problem

//Exercise 1

function billTotal(subTotal) {
    var taxRate = 0.095;
  var tipRate = 0.15;
  return subTotal + (subTotal * taxRate) + (subTotal * tipRate);
}
//billTotal(30);=>37.35



//Exercise 2 - not finished

var animal = (cat, dog)
var emotions = (angry, happy, sad, surprised)
var catNoise = ["hiss","purr","murp","meow-meow"]
var dogNoise = ["grrr","roof","erp","bark"]

function animalNoise(animal, emotion) {
 if (animal === cat) {
  return
 }
 else if (animal === dog) {
  return

}


//Exercise 3 - didnt get to it
